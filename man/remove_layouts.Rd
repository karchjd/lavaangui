% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove_layouts.R
\name{remove_layouts}
\alias{remove_layouts}
\title{Remove Saved Layouts for a Lavaan Model}
\usage{
remove_layouts(fit, layout = NULL)
}
\arguments{
\item{fit}{A lavaan model, as returned by the \code{lavaan}, \code{sem}, or \code{cfa} functions from the \code{lavaan} package.}

\item{layout}{A character string specifying which layout to remove.
If \code{NULL} (the default), all saved layouts for the model are removed.
If a character string, only the layout with that name is removed.}
}
\value{
Invisibly returns the paths of removed files.
}
\description{
Removes layout files previously saved by \code{\link[=plot_lavaan]{plot_lavaan()}} for a given lavaan model.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
library(lavaan)
model <- "
  visual  =~ x1 + x2 + x3
  textual =~ x4 + x5 + x6
  speed   =~ x7 + x8 + x9
"
fit <- cfa(model, data = HolzingerSwineford1939)
remove_layouts(fit)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=plot_lavaan]{plot_lavaan()}} to create and interactively edit path diagrams,
\code{\link[=export_plot]{export_plot()}} to export diagrams to a file without interaction.
}
